#include <stdio.h>

int main() {
    int N, Ci, Vi, i;

    while (1) {
        scanf("%d", &N);
        if(N == 0) break;

        for (i = 0; i < N; i++) {
            scanf("%d %d", &Ci, &Vi);
            
        }
    }
    
    return 0;
}